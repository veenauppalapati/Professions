function checkboxHandler (){

	// PANEL THAT HOLDS ALL THE CHECKBOXES
	//======================================
	// creating a div to hold all the checkboxes
	var newPanel = $('<div>');
	// add a class to it
	newPanel.addClass('panel panel-default');

		// CHECKBOX WITH A NAME
		//======================================

		var fieldPanel = $('<div>');
		var input = $('<input[type="checkbox"]>');
		input.text('FILMMAKER');



};